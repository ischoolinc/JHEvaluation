
# 🎯 目標

將「一般領域（特別是語文）」科目數上限由 6 調整為 10，確保：

* DataTable 欄位完整建立
* 成績 / 等第 / 權數 正確寫入
* MailMerge 可正常對應
* 不影響既有邏輯

---

# ✅ Step 1：新增常數（避免硬編碼）

📍檔案：PrintForm_StudentScoreCertificattion_English.cs

在 class 內新增：

```csharp
const int NormalDomainSubjectMax = 10;
```

---

# ✅ Step 2：修改 DataTable 欄位建立（科目）

📍搜尋關鍵：

```csharp
for (int a = 1; a <= 6; a++)
```

📍修改為：

```csharp
for (int a = 1; a <= NormalDomainSubjectMax; a++)
```

📌 套用範圍：

* domain + "_科目名稱"
* domain + "_科目X_權數"
* domain + "_科目X_成績"
* domain + "_科目X_等第"
* domain + "_科目X_原始成績"
* domain + "_科目X_原始等第"
* domain + "_科目X_平均成績"
* domain + "_科目X_平均成績等第"

⚠️ 注意：
只改「一般領域」，不要動「彈性課程 7~18」那段

---

# ✅ Step 3：修改 subjectScoreType_list

📍搜尋：

```csharp
for (int a = 1; a <= 6; a++)
```

📍修改為：

```csharp
for (int a = 1; a <= NormalDomainSubjectMax; a++)
```

📌 影響：

* _成績
* _原始成績
* _平均成績

---

# ✅ Step 4：修改 subjectLevelType_list

📍搜尋：

```csharp
for (int a = 1; a <= 6; a++)
```

📍修改為：

```csharp
for (int a = 1; a <= NormalDomainSubjectMax; a++)
```

📌 影響：

* _等第
* _原始等第
* _平均成績等第

---

# ✅ Step 5：修改 subjectCredit_list

📍搜尋：

```csharp
for (int a = 1; a <= 6; a++)
```

📍修改為：

```csharp
for (int a = 1; a <= NormalDomainSubjectMax; a++)
```

📌 影響：

* _權數

---

# ✅ Step 6：修改科目數上限判斷（最重要）

📍搜尋：

```csharp
if (subjectCourseCount > 6)
```

📍修改為：

```csharp
if (subjectCourseCount > NormalDomainSubjectMax)
```

📌 否則第7~10科目會被忽略

---

# ⚠️ Step 7：Word 樣板同步修改（必要）

📍需新增以下欄位：

語文_科目名稱7 ~ 10

語文_科目7~10：

* 權數1~6
* 成績1~6
* 等第1~6
* 原始成績1~6
* 原始等第1~6
* 平均成績
* 平均成績等第

📌 若未新增：
👉 程式有資料，但報表不會顯示

---

# ✅ 測試案例

1️⃣ 語文科目 6 科 → 正常顯示
2️⃣ 語文科目 7~10 科 → 必須全部顯示
3️⃣ 語文科目 >10 → 顯示前10 + 警告訊息
4️⃣ 非語文領域（數學等） → 不受影響

---

# 🧾 修改完成紀錄

檔案名稱：
在校成績證明書英文新版調整0415.md

內容建議：

* 調整語文科目上限由 6 → 10
* 新增 NormalDomainSubjectMax 常數
* 修改 DataTable 欄位建立
* 修改 subjectScoreType / subjectLevelType / subjectCredit
* 修正科目數上限判斷
* Word 樣板同步新增欄位
