【2026-04-20】
1. 調整 `PrintForm_StudentScoreCertificattion.cs`
2. 一般領域科目數由 6 → 10（比照英文版）
3. 彈性課程維持 18 科不變
4. 調整範圍：
   - 欄位建立（Columns）
   - 科目成績清單（`subjectScoreType_list`）
   - 科目等第清單（`subjectLevelType_list`）
   - 科目權數清單（`subjectCredit_list`）
   - 同領域科目數上限判斷（`subjectCourseCount`）
5. 採保守修改，未影響其他邏輯

6. 在 `Util.cs` 新增 `SubjectOrdinalInfo`
7. 在 `Util.cs` 新增共用方法：
   - `GetSubjectOrdinalList()`
   - `GetSubjectOrdinalDict()`
   - `GetSubjectEnglishNameDict()`
8. 共用資料來源：
   - `list.name = 'JHEvaluation_Subject_Ordinal'`
9. 解析 `SubjectOrdinal` escaped XML，取得：
   - 科目排序
   - 科目中文名稱
   - 科目英文名稱
10. `PrintForm_StudentScoreCertificattion.cs` 改為透過 `Util.GetSubjectOrdinalDict()` 進行科目排序
11. `PrintForm_StudentScoreCertificattion_English.cs` 改為透過：
   - `Util.GetSubjectOrdinalDict()`
   - `Util.GetSubjectEnglishNameDict()`
     共用排序與英文名稱
12. 採保守修改：
   - SQL 集中於 `Util.cs`
   - 查無資料時不拋錯
   - 不影響其他成績與報表邏輯

