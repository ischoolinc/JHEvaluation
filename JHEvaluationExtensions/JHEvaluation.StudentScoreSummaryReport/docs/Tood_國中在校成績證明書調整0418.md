# Cursor Todo.md
# 目標：
# 修改 PrintForm_StudentScoreCertificattion.cs
# 將「原始成績 / 原始等第」由 Score 改為 ScoreOrigin（含空值保護）
# 參考：PrintForm_StudentScoreCertificattion_English.cs
# 注意：不得影響既有「成績 / 等第」邏輯

--------------------------------------------------
【修改檔案】
PrintForm_StudentScoreCertificattion.cs

--------------------------------------------------
【修改規則】

一、原始成績（全部統一）
將：
    subjectscore.Value.Score

改為：
    subjectscore.Value.ScoreOrigin.HasValue 
        ? subjectscore.Value.ScoreOrigin.Value 
        : (decimal?)null

--------------------------------------------------
二、原始等第（全部統一）

將：
    _ScoreMappingConfig.ParseScoreName(subjectscore.Value.Score)

改為：
    subjectscore.Value.ScoreOrigin.HasValue 
        ? _ScoreMappingConfig.ParseScoreName(subjectscore.Value.ScoreOrigin.Value) 
        : ""

--------------------------------------------------
【實際修改位置（共8處）】

========（1）單數學期：彈性課程========
搜尋：
    "_原始成績" + (grade * 2 - 1)

修改：
    = subjectscore.Value.Score;
→
    = subjectscore.Value.ScoreOrigin.HasValue 
        ? subjectscore.Value.ScoreOrigin.Value 
        : (decimal?)null;

----------------------------------------
搜尋：
    "_原始等第" + (grade * 2 - 1)

修改：
    = _ScoreMappingConfig.ParseScoreName(subjectscore.Value.Score);
→
    = subjectscore.Value.ScoreOrigin.HasValue 
        ? _ScoreMappingConfig.ParseScoreName(subjectscore.Value.ScoreOrigin.Value) 
        : "";

==================================================
（2）單數學期：一般科目

搜尋：
    Domain + "_科目" + SubjectCourseNum + "_原始成績" + (grade * 2 - 1)

修改同上（Score → ScoreOrigin）

----------------------------------------
搜尋：
    "_原始等第" + (grade * 2 - 1)

修改同上（ParseScoreName → ScoreOrigin）

==================================================
（3）雙數學期：彈性課程

搜尋：
    "_原始成績" + (grade * 2)

修改同上

----------------------------------------
搜尋：
    "_原始等第" + (grade * 2)

修改同上

==================================================
（4）雙數學期：一般科目

搜尋：
    Domain + "_科目" + SubjectCourseNum + "_原始成績" + (grade * 2)

修改同上

----------------------------------------
搜尋：
    "_原始等第" + (grade * 2)

修改同上

--------------------------------------------------
【重要注意事項（務必遵守）】

1. 不可修改以下邏輯：
   ✔ subjectscore.Value.Score（正式成績）
   ✔ ParseScoreName(subjectscore.Value.Score)（正式等第）

2. 僅限修改：
   ✔ 原始成績
   ✔ 原始等第

3. 必須保留 HasValue 判斷，避免 null exception

4. decimal 型別請維持：
   (decimal?)null
   （不可改成 0 或 ""）

--------------------------------------------------
【修改完成後驗證】

1. 原始成績有值 → 正確顯示
2. 原始成績為 null → 不顯示（不報錯）
3. 等第轉換正常
4. 報表可正常產生（PDF / Word）

--------------------------------------------------
【修改完成紀錄】

請新增或更新檔案：
國中在校成績證明書調整0418.md

內容建議：

---
# 國中在校成績證明書調整0418

## 調整項目
1. 原始成績由 Score 改為 ScoreOrigin
2. 原始等第由 Score 改為 ScoreOrigin
3. 增加 ScoreOrigin 空值保護（HasValue 判斷）

## 影響範圍
- PrintForm_StudentScoreCertificattion.cs
- 僅影響「原始成績」欄位
- 不影響既有成績與等第邏輯

## 驗證結果
- 報表正常
- 無 null exception
- 原始成績顯示正確

---
