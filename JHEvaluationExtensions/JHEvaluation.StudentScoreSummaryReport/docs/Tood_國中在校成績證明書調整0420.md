# 任務名稱
調整 PrintForm_StudentScoreCertificattion.cs 領域科目數由 6 → 10（比照英文版）

# 目標
1. 一般領域科目數由 6 改為 10
2. 彈性課程維持 18 科（不可變動）
3. 不影響：
   - 領域成績（6學期）
   - 學習領域成績
   - 其他資料流
4. 修改完成後記錄在：
   在校成績證明書調整0420.md

--------------------------------------------------

# Step 1：新增常數（避免魔法數字）

【檔案】
PrintForm_StudentScoreCertificattion.cs

【新增位置】
class 內欄位區

【新增程式碼】

const int NormalDomainSubjectMax = 10;

--------------------------------------------------

# Step 2：建立欄位（Columns）6 → 動態

【搜尋關鍵字】
for (int a = 1; a <= 6; a++)

【原始寫法（需修改）】

foreach (string domain in DomainList)
{
    for (int a = 1; a <= 6; a++)
    {
        ...
    }

    if (domain == "彈性課程")
        for (int a = 7; a <= 18; a++)
        {
            ...
        }
}

【修改後】

foreach (string domain in DomainList)
{
    int subjectMax =
        domain == "彈性課程" ? 18 :
        NormalDomainSubjectMax;

    for (int a = 1; a <= subjectMax; a++)
    {
        table.Columns.Add(domain + "_科目名稱" + a);

        for (int i = 1; i <= 6; i++)
        {
            table.Columns.Add(domain + "_科目" + a + "_權數" + i);
            table.Columns.Add(domain + "_科目" + a + "_成績" + i);
            table.Columns.Add(domain + "_科目" + a + "_等第" + i);
            table.Columns.Add(domain + "_科目" + a + "_原始成績" + i);
            table.Columns.Add(domain + "_科目" + a + "_原始等第" + i);
        }

        table.Columns.Add(domain + "_科目" + a + "_平均成績");
        table.Columns.Add(domain + "_科目" + a + "_平均成績等第");
    }
}

【注意】
刪除原本：
for (int a = 7; a <= 18; a++) 相關彈性課程補段

--------------------------------------------------

# Step 3：科目成績清單（subjectScoreType_list）

【搜尋】
subjectScoreType_list

【原本】
1~6 + 彈性課程 7~18 分開寫

【修改後】

foreach (string domain in DomainList)
{
    int subjectMax =
        domain == "彈性課程" ? 18 :
        NormalDomainSubjectMax;

    for (int a = 1; a <= subjectMax; a++)
    {
        subjectScoreType_list.Add(domain + "_科目" + a + "_成績");
        subjectScoreType_list.Add(domain + "_科目" + a + "_原始成績");
        subjectScoreType_list.Add(domain + "_科目" + a + "_平均成績");
    }
}

【注意】
刪除彈性課程 7~18 補段

--------------------------------------------------

# Step 4：科目等第清單（subjectLevelType_list）

【搜尋】
subjectLevelType_list

【修改後】

foreach (string domain in DomainList)
{
    int subjectMax =
        domain == "彈性課程" ? 18 :
        NormalDomainSubjectMax;

    for (int a = 1; a <= subjectMax; a++)
    {
        subjectLevelType_list.Add(domain + "_科目" + a + "_等第");
        subjectLevelType_list.Add(domain + "_科目" + a + "_原始等第");
        subjectLevelType_list.Add(domain + "_科目" + a + "_平均成績等第");
    }
}

--------------------------------------------------

# Step 5：科目權數清單（subjectCredit_list）

【搜尋】
subjectCredit_list

【修改後】

foreach (string domain in DomainList)
{
    int subjectMax =
        domain == "彈性課程" ? 18 :
        NormalDomainSubjectMax;

    for (int a = 1; a <= subjectMax; a++)
    {
        subjectCredit_list.Add(domain + "_科目" + a + "_權數");
    }
}

--------------------------------------------------

# Step 6：同領域科目數上限（最重要）

【搜尋】
subjectCourseCount > 6

【原本】

if (subjectCourseCount > 6)
{
    isExceed = true;
    continue;
}

【修改後】

int subjectCourseMax = NormalDomainSubjectMax;

if (subjectCourseCount > subjectCourseMax)
{
    isExceed = true;
    continue;
}

--------------------------------------------------

# Step 7：驗證（務必測試）

【測試案例】

1. 一般領域 = 6 科
   → 結果應完全相同（回歸測試）

2. 一般領域 = 7~10 科
   → 第 7~10 科要正確顯示

3. 一般領域 > 10 科
   → 只顯示前 10 科
   → 不可拋錯

4. 彈性課程
   → 必須仍為 18 科

--------------------------------------------------

# Step 8：修改記錄

請新增或更新檔案：

在校成績證明書調整0420.md

內容建議：

【2026-04-20】
1. 調整 PrintForm_StudentScoreCertificattion.cs
2. 一般領域科目數由 6 → 10（比照英文版）
3. 彈性課程維持 18 科不變
4. 調整範圍：
   - 欄位建立
   - 科目成績清單
   - 科目等第清單
   - 科目權數清單
   - 同領域科目數上限判斷
5. 採保守修改，未影響其他邏輯
