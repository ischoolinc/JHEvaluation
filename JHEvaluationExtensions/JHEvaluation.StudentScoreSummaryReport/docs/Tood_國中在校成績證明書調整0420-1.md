# Cursor Todo.md
## 任務：在 Util.cs 新增 SubjectOrdinal 共用 SQL 與方法，讓 PrintForm_StudentScoreCertificattion.cs、PrintForm_StudentScoreCertificattion_English.cs 共用
## 修改完成後記錄在：在校成績證明書調整0420.md

--------------------------------------------------
# 一、目標
1. 在 `Util.cs` 新增共用 SQL 與方法，讀取：
   - 科目排序 `SubjectOrdinal`
   - 科目中文名稱 `SubjectName`
   - 科目英文名稱 `EnglishName`
2. 讓以下兩支程式共用，不要各自重寫 SQL：
   - `PrintForm_StudentScoreCertificattion.cs`
   - `PrintForm_StudentScoreCertificattion_English.cs`
3. 採保守安全方式：
   - 不破壞既有 `GetDomainList()` 等方法
   - 不更動其他與成績計算無關邏輯
   - 若查不到資料，呼叫端仍可 fallback 使用原本流程
4. 修改完成後，將調整內容記錄在：
   - `在校成績證明書調整0420.md`

--------------------------------------------------
# 二、先理解資料來源
資料來源為：

```sql
select content
from list
where name = 'JHEvaluation_Subject_Ordinal';
````

`content` 是外層 XML，裡面 `Configuration Name="SubjectOrdinal"` 的內容是 escaped XML。
需先：

1. 取出 `SubjectOrdinal` 的 text
2. 將 `&lt;`, `&gt;`, `&quot;`, `&amp;` 還原
3. 再使用 `xpath + unnest + with ordinality` 解析

---

# 三、在 Util.cs 新增資料類別

## 1. 在 `Util.cs` 的 `Util` 類別內，新增一個資料類別

請新增：

```csharp
public class SubjectOrdinalInfo
{
    public int SubjectOrdinal { get; set; }
    public string SubjectName { get; set; }
    public string EnglishName { get; set; }
}
```

## 注意

1. 命名使用 `SubjectOrdinalInfo`
2. 屬性名稱固定：

   * `SubjectOrdinal`
   * `SubjectName`
   * `EnglishName`
3. 請放在 `Util.cs` 裡，位置可比照其他小型資料結構或 helper 附近
4. 不要改名成其他名稱，避免之後兩支報表共用時再調整

---

# 四、在 Util.cs 新增共用方法：GetSubjectOrdinalList()

## 1. 新增方法

請在 `Util.cs` 新增以下方法：

```csharp
/// <summary>
/// 取得科目排序與英文名稱對照。
/// 來源：list.name = 'JHEvaluation_Subject_Ordinal'
/// </summary>
/// <returns></returns>
public static List<SubjectOrdinalInfo> GetSubjectOrdinalList()
{
    List<SubjectOrdinalInfo> value = new List<SubjectOrdinalInfo>();

    try
    {
        QueryHelper qh = new QueryHelper();
        string query = @"
WITH src AS (
    SELECT xmlparse(document content) AS outer_xml
    FROM list
    WHERE name = 'JHEvaluation_Subject_Ordinal'
),
raw_txt AS (
    SELECT
        (xpath('/Configurations/Configuration[@Name=""SubjectOrdinal""]/text()', outer_xml))[1]::text AS txt
    FROM src
),
decoded_xml AS (
    SELECT xmlparse(document
        replace(
            replace(
                replace(
                    replace(txt, '&lt;', '<'),
                '&gt;', '>'),
            '&quot;', '""'),
        '&amp;', '&')
    ) AS inner_xml
    FROM raw_txt
)
SELECT
    u.ordinal AS subject_ordinal,
    COALESCE((xpath('@Name', u.node))[1]::text, '') AS subject_name,
    COALESCE((xpath('@EnglishName', u.node))[1]::text, '') AS english_name
FROM decoded_xml d
CROSS JOIN LATERAL unnest(xpath('/Subjects/Subject', d.inner_xml))
    WITH ORDINALITY AS u(node, ordinal)
ORDER BY u.ordinal;
";

        DataTable dt = qh.Select(query);

        foreach (DataRow dr in dt.Rows)
        {
            SubjectOrdinalInfo info = new SubjectOrdinalInfo();

            int ordinal;
            if (!int.TryParse(dr["subject_ordinal"].ToString(), out ordinal))
                ordinal = 0;

            info.SubjectOrdinal = ordinal;
            info.SubjectName = dr["subject_name"].ToString();
            info.EnglishName = dr["english_name"].ToString();

            value.Add(info);
        }
    }
    catch (Exception ex)
    {
        // 讀取科目排序設定失敗時，回傳空集合，讓呼叫端可自行 fallback。
    }

    return value;
}
```

## 2. 注意事項

1. SQL 請保持原樣，不要自行改成其他 XML 寫法
2. 保持 `xpath + unnest + WITH ORDINALITY`
3. `replace` 順序不要改

   * `&lt;`
   * `&gt;`
   * `&quot;`
   * `&amp;`
4. `catch` 先維持保守不拋錯，避免影響既有報表流程
5. 若資料表無資料，應回傳空 `List<SubjectOrdinalInfo>()`

---

# 五、在 Util.cs 新增 Dictionary helper 方法

## 1. 新增 GetSubjectOrdinalDict()

```csharp
/// <summary>
/// 取得科目名稱 -> 排序 的對照表
/// </summary>
/// <returns></returns>
public static Dictionary<string, int> GetSubjectOrdinalDict()
{
    Dictionary<string, int> value = new Dictionary<string, int>();

    foreach (SubjectOrdinalInfo info in GetSubjectOrdinalList())
    {
        if (!string.IsNullOrEmpty(info.SubjectName) && !value.ContainsKey(info.SubjectName))
            value.Add(info.SubjectName, info.SubjectOrdinal);
    }

    return value;
}
```

## 2. 新增 GetSubjectEnglishNameDict()

```csharp
/// <summary>
/// 取得科目名稱 -> 英文名稱 的對照表
/// </summary>
/// <returns></returns>
public static Dictionary<string, string> GetSubjectEnglishNameDict()
{
    Dictionary<string, string> value = new Dictionary<string, string>();

    foreach (SubjectOrdinalInfo info in GetSubjectOrdinalList())
    {
        if (!string.IsNullOrEmpty(info.SubjectName) && !value.ContainsKey(info.SubjectName))
            value.Add(info.SubjectName, info.EnglishName);
    }

    return value;
}
```

## 注意

1. 這兩支方法是讓兩支報表共用，避免各自再轉換一次
2. 若重複科目名稱，保留第一筆即可
3. 不要加入多餘邏輯，例如 trim、ToLower、重新排序，避免影響原始資料設定

---

# 六、修改 PrintForm_StudentScoreCertificattion.cs

## 目標

讓中文版報表改為透過 `Util.GetSubjectOrdinalDict()` 取得科目排序設定。

## 1. 搜尋目前「領域內科目排序」相關邏輯

請找出目前 `PrintForm_StudentScoreCertificattion.cs` 中：

* 科目收集
* 科目排序
* 或依首次出現順序決定科目位置
  的程式碼

## 2. 加入共用排序 Dictionary

在適當位置新增：

```csharp
Dictionary<string, int> subjectOrdinalDict = Util.GetSubjectOrdinalDict();
```

## 3. 若目前有 subjects.Sort(...) 或類似排序

請改成優先依 `subjectOrdinalDict` 排序。
建議保守寫法如下：

```csharp
subjects.Sort(delegate(SubjectScore x, SubjectScore y)
{
    int xOrder = subjectOrdinalDict.ContainsKey(x.Name) ? subjectOrdinalDict[x.Name] : int.MaxValue;
    int yOrder = subjectOrdinalDict.ContainsKey(y.Name) ? subjectOrdinalDict[y.Name] : int.MaxValue;

    int result = xOrder.CompareTo(yOrder);
    if (result == 0)
        result = x.Name.CompareTo(y.Name);

    return result;
});
```

## 注意

1. 只改「領域內科目排序」相關
2. 不要改動其他成績、欄位、平均計算
3. 若該科目不在 `subjectOrdinalDict`，就排到後面
4. fallback 規則為 `int.MaxValue`，不要丟例外
5. 若目前程式不是 `subjects.Sort(...)` 而是其他 collection，請套用同樣排序概念，不要硬套變數名稱

---

# 七、修改 PrintForm_StudentScoreCertificattion_English.cs

## 目標

讓英文版報表改為透過：

* `Util.GetSubjectOrdinalDict()`
* `Util.GetSubjectEnglishNameDict()`
  取得共用設定

## 1. 在適當位置新增：

```csharp
Dictionary<string, int> subjectOrdinalDict = Util.GetSubjectOrdinalDict();
Dictionary<string, string> subjectEnglishNameDict = Util.GetSubjectEnglishNameDict();
```

## 2. 領域內科目排序改為優先依 `subjectOrdinalDict`

排序邏輯可比照中文版同寫法：

```csharp
subjects.Sort(delegate(SubjectScore x, SubjectScore y)
{
    int xOrder = subjectOrdinalDict.ContainsKey(x.Name) ? subjectOrdinalDict[x.Name] : int.MaxValue;
    int yOrder = subjectOrdinalDict.ContainsKey(y.Name) ? subjectOrdinalDict[y.Name] : int.MaxValue;

    int result = xOrder.CompareTo(yOrder);
    if (result == 0)
        result = x.Name.CompareTo(y.Name);

    return result;
});
```

## 3. 英文名稱取得改為透過共用 Dictionary

若目前英文版有硬編碼或自行轉英文名稱，請改成優先讀：

```csharp
string englishName = "";
if (subjectEnglishNameDict.ContainsKey(subjectName))
    englishName = subjectEnglishNameDict[subjectName];
```

## 注意

1. 保守修改，僅改英文名稱取得來源與排序來源
2. 若找不到英文名稱，不要中斷流程
3. 找不到英文名稱時，維持原本 fallback 行為
4. 不要改動英文版其他欄位、科目數、平均、等第等邏輯

---

# 八、重要保守原則

1. 不要把 SQL 直接散寫進 `PrintForm_StudentScoreCertificattion.cs`
2. 不要把 SQL 直接散寫進 `PrintForm_StudentScoreCertificattion_English.cs`
3. 共用查詢只放在 `Util.cs`
4. 若查不到 `JHEvaluation_Subject_Ordinal`

   * 不可整個報表拋錯
   * 需回傳空集合，讓呼叫端 fallback
5. 既有邏輯若原本是「首次出現順序」

   * 當 dictionary 為空時，應可維持原本流程，不可強行改壞

---

# 九、建議驗證項目

## 1. SQL 層驗證

先直接測 `Util.cs` 內 SQL 是否可正確取回：

* `subject_ordinal`
* `subject_name`
* `english_name`

## 2. 中文版報表驗證

確認：

1. 領域內科目排序依 `JHEvaluation_Subject_Ordinal`
2. 若 SQL 無資料，不會報錯
3. 原本成績顯示、平均、等第不受影響

## 3. 英文版報表驗證

確認：

1. 領域內科目排序依 `JHEvaluation_Subject_Ordinal`
2. 英文科目名稱優先取 `EnglishName`
3. 若某科目無英文名稱，不會報錯
4. 原本 10 科 / 彈性課程 18 科邏輯不受影響

## 4. 特別檢查

至少檢查這幾個科目：

* 國語文 → Chinese
* 英語文 / 英文 → English
* 數學 → Mathematics
* 歷史 → History
* 本土語文/臺灣手語 → Native Language/Taiwan Sign Language
* 閩南語文 → Taiwanese Hokkien Language

---

# 十、修改完成後記錄

請更新：

`在校成績證明書調整0420.md`

建議記錄內容如下：

## 2026-04-20

1. 在 `Util.cs` 新增 `SubjectOrdinalInfo`
2. 在 `Util.cs` 新增共用方法：

   * `GetSubjectOrdinalList()`
   * `GetSubjectOrdinalDict()`
   * `GetSubjectEnglishNameDict()`
3. 共用資料來源：

   * `list.name = 'JHEvaluation_Subject_Ordinal'`
4. 解析 `SubjectOrdinal` escaped XML，取得：

   * 科目排序
   * 科目中文名稱
   * 科目英文名稱
5. `PrintForm_StudentScoreCertificattion.cs` 改為透過 `Util.GetSubjectOrdinalDict()` 進行科目排序
6. `PrintForm_StudentScoreCertificattion_English.cs` 改為透過：

   * `Util.GetSubjectOrdinalDict()`
   * `Util.GetSubjectEnglishNameDict()`
     共用排序與英文名稱
7. 採保守修改：

   * SQL 集中於 `Util.cs`
   * 查無資料時不拋錯
   * 不影響其他成績與報表邏輯