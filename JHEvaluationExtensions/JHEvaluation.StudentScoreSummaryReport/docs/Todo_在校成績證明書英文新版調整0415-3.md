# 🎯 目標

調整英文版在校成績證明書：

1. **除了彈性課程以外，其他所有一般領域科目數都由 6 改成 10**

   * 不再只有「語文」支援到 10
   * 例如：數學、社會、自然科學、藝術、健康與體育、綜合活動、科技…等，也都支援到 10
2. **彈性課程維持原本特殊邏輯**

   * 仍維持 1~18 的既有處理方式
3. **中文版邏輯不要更動**
4. **畫面呼叫方式不要更動**
5. **修改完成後記錄在 `在校成績證明書英文新版調整0415.md`**

---

# 📍 修改檔案

* `PrintForm_StudentScoreCertificattion_English.cs`
* `Global.cs`

---

# ✅ 一、修改 `PrintForm_StudentScoreCertificattion_English.cs`

## Step 1：保留常數

確認 class 內保留：

```csharp
const int NormalDomainSubjectMax = 10;
```

---

## Step 2：調整 DataTable 科目欄位建立邏輯

📍搜尋目前這段：

```csharp
int subjectMax =
    domain == "彈性課程" ? 6 :
    domain == "語文" ? NormalDomainSubjectMax :
    6;
```

📍改成：

```csharp
int subjectMax =
    domain == "彈性課程" ? 6 :
    NormalDomainSubjectMax;
```

📌 目的：

* 除了彈性課程外，其餘一般領域全部支援到 10

📌 影響範圍：

* `domain + "_科目名稱1~10"`
* `domain + "_科目1~10_權數1~6"`
* `domain + "_科目1~10_成績1~6"`
* `domain + "_科目1~10_等第1~6"`
* `domain + "_科目1~10_原始成績1~6"`
* `domain + "_科目1~10_原始等第1~6"`
* `domain + "_科目1~10_平均成績"`
* `domain + "_科目1~10_平均成績等第"`

---

## Step 3：調整 `subjectScoreType_list`

📍搜尋目前這段：

```csharp
int subjectMax =
    domain == "彈性課程" ? 6 :
    domain == "語文" ? NormalDomainSubjectMax :
    6;
```

📍改成：

```csharp
int subjectMax =
    domain == "彈性課程" ? 6 :
    NormalDomainSubjectMax;
```

📌 目的：
讓所有一般領域都建立到 `科目1~10_成績 / 原始成績 / 平均成績`

---

## Step 4：調整 `subjectLevelType_list`

📍搜尋同樣寫法：

```csharp
int subjectMax =
    domain == "彈性課程" ? 6 :
    domain == "語文" ? NormalDomainSubjectMax :
    6;
```

📍改成：

```csharp
int subjectMax =
    domain == "彈性課程" ? 6 :
    NormalDomainSubjectMax;
```

📌 目的：
讓所有一般領域都建立到 `科目1~10_等第 / 原始等第 / 平均成績等第`

---

## Step 5：調整 `subjectCredit_list`

📍搜尋同樣寫法：

```csharp
int subjectMax =
    domain == "彈性課程" ? 6 :
    domain == "語文" ? NormalDomainSubjectMax :
    6;
```

📍改成：

```csharp
int subjectMax =
    domain == "彈性課程" ? 6 :
    NormalDomainSubjectMax;
```

📌 目的：
讓所有一般領域都建立到 `科目1~10_權數`

---

## Step 6：調整一般領域科目上限判斷

📍搜尋目前這段：

```csharp
int subjectCourseMax = subjectscore.Value.Domain == "語文" ? NormalDomainSubjectMax : 6;
```

📍改成：

```csharp
int subjectCourseMax = NormalDomainSubjectMax;
```

📌 目的：
目前只有語文可超過 6，其他會被卡住。
改完後，除彈性課程外，所有一般領域最多都可到 10。

⚠️ 注意：
這段本來就在「非彈性課程 / 非彈性學習 / 非空字串」的分支內，所以這裡直接改成 `NormalDomainSubjectMax` 即可，不會影響彈性課程。

---

## Step 7：保留保守安全修正

確認保留這些 `table.Columns.Contains(key)` 判斷，不可拿掉：

* `domainScore_dict`
* `domainLevel_dict`
* `domainCredit_dict`
* `subjectScore_dict`
* `subjectLevel_dict`
* `subjectCredit_dict`

📌 原因：
避免再發生：

* `資料行 '領域_彈性學習課程_平均成績' 不屬於資料表`

---

# ✅ 二、修改 `Global.cs`

## Step 8：保留現有中英文分流檔名

保留目前：

```csharp
string inputReportName = "在校成績證明書合併欄位總表_中文版";

if (doc == "英文版")
{
    inputReportName = "在校成績證明書合併欄位總表_英文版";
}
```

📌 不要改畫面呼叫方式

---

## Step 9：調整英文版合併欄位總表「學期科目成績」區塊

📍搜尋目前這段：

```csharp
int domainSubjectMax = (doc == "英文版" && domain == "語文") ? subjectMax : DefaultDomainSubjectMax;
```

📍改成：

```csharp
int domainSubjectMax = (doc == "英文版" && domain != "彈性課程") ? subjectMax : DefaultDomainSubjectMax;
```

📌 目的：
英文版合併欄位總表中，除了彈性課程外，所有一般領域都顯示到 `科目1~10`

⚠️ 注意：
`彈性課程` 後面本來還有 `7~18` 特殊段，不能破壞。

---

## Step 10：調整英文版合併欄位總表「學期科目原始成績」區塊

📍搜尋同樣這段：

```csharp
int domainSubjectMax = (doc == "英文版" && domain == "語文") ? subjectMax : DefaultDomainSubjectMax;
```

📍改成：

```csharp
int domainSubjectMax = (doc == "英文版" && domain != "彈性課程") ? subjectMax : DefaultDomainSubjectMax;
```

📌 目的：
英文版總表中，所有一般領域原始成績欄位都支援到 `科目1~10`

---

# ⚠️ 三、不要修改的地方

## 1. 不要改畫面呼叫方式

保留：

```csharp
Global.ExportMappingFieldWord("英文版");
```

---

## 2. 不要改中文版邏輯

中文版仍維持：

* 一般領域 `1~6`
* 彈性課程特殊邏輯不變

---

## 3. 不要改彈性課程特殊段

以下邏輯保留：

* `PrintForm_StudentScoreCertificattion_English.cs` 內 `彈性課程 7~18`
* `Global.cs` 內 `彈性課程 7~18`

不可刪除、不可混入一般領域 1~10 邏輯。

---

# ✅ 四、測試案例

## 測試 1：英文版一般領域

* 語文 10 科
* 數學 8 科
* 社會 7 科
* 自然科學 9 科

結果：

* 全部都可正常顯示
* 不可只剩語文可到 10

---

## 測試 2：英文版彈性課程

* 彈性課程超過 6 科

結果：

* 仍走原本彈性課程特殊邏輯
* 不可被一般領域 1~10 邏輯覆蓋

---

## 測試 3：中文版

結果：

* 一般領域仍維持 1~6
* 不可被這次英文版改動影響

---

## 測試 4：避免欄位不存在錯誤

資料含：

* `彈性學習課程`

結果：

* 不可再出現 `資料行不屬於資料表`
* 報表可完成

---

# 🧾 修改完成紀錄

檔案名稱：
`在校成績證明書英文新版調整0415.md`

內容建議：

```md
## 英文版一般領域科目數擴充
- 原本只有語文科目數支援到 10
- 調整為：除了彈性課程外，其餘一般領域全部支援到 10
- 修改檔案：
  - PrintForm_StudentScoreCertificattion_English.cs
  - Global.cs

## PrintForm_StudentScoreCertificattion_English.cs
- 調整 DataTable 科目欄位建立邏輯
- 調整 subjectScoreType_list
- 調整 subjectLevelType_list
- 調整 subjectCredit_list
- 調整一般領域科目上限判斷
- 保留 table.Columns.Contains(key) 安全判斷

## Global.cs
- 英文版合併欄位總表調整為：除彈性課程外，一般領域全部支援到 10
- 中文版維持原本 1~6
- 保留中英文分開產生檔名
- 保留畫面呼叫方式不變
```
