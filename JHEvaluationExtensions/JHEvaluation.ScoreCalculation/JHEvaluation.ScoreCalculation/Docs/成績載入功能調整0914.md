# 成績載入功能調整（2026-09-14）

## 1. 修改檔案

* `Program.cs`

僅註解三筆功能選單註冊。未刪除相關類別，未改計算邏輯、ACL、`mbAdmin.Enable`、學生選取事件、畢業作業或學期歷程。

## 2. 停用的三個功能

1. 學生端：`加總學習領域文字描述`
2. 教務批次：`批次計算學習領域成績`
3. 教務批次：`批次加總學習領域文字描述`

## 3. 已註解的註冊程式

```csharp
// 暫停使用：加總學習領域文字描述
//mb["加總學習領域文字描述"].Click += delegate { new DomainTextScoreSum(NLDPanels.Student.SelectedSource).ShowDialog(); };

// 暫停使用：批次計算學習領域成績
//mbAdmin["批次計算學習領域成績"].Click += delegate
//{ new LearningDomainScoreCalculateByGradeyear().ShowDialog(); };

// 暫停使用：批次加總學習領域文字描述
//mbAdmin["批次加總學習領域文字描述"].Click += delegate
//{ new DomainTextScoreSumByGradeyear().ShowDialog(); };
```

學生端原本是單行寫法，註解後仍維持單行；兩筆批次功能維持原本的兩行寫法。

## 4. 刻意維持不變的功能

學生端：

* `計算科目成績` → `SubjectScoreCalculate`
* `計算領域成績` → `DomainScoreCalculate`
* `計算學習領域成績` → `LearningDomainScoreCalculate`

教務批次：

* `批次計算科目成績` → `SubjectScoreCalculateByGradeyear`
* `批次計算領域成績` → `DomainScoreCalculateByGradeyear`

## 5. 相關計算類別未刪除

下列類別與專案檔項目仍保留：

* `DomainTextScoreSum` / `DomainTextScoreSum.Designer.cs` / `DomainTextScoreSum.resx`
* `LearningDomainScoreCalculateByGradeyear` / `LearningDomainScoreCalculateByGradeyear.Designer.cs` / `LearningDomainScoreCalculateByGradeyear.resx`
* `DomainTextScoreSumByGradeyear` / `DomainTextScoreSumByGradeyear.Designer.cs` / `DomainTextScoreSumByGradeyear.resx`

## 6. 建置結果

`MSBuild` Debug / AnyCPU 建置成功，exit code 0。

輸出：`bin\debug\JHEvaluation.ScoreCalculation.dll`

無因本次註解產生的編譯錯誤。建置過程僅有既有 warning（重複 using、未使用變數、過時 API 等），與本次選單註解無關。

## 7. 學生選單驗證

靜態檢查 `Program.cs`：

```text
學生 → 教務 → 成績作業
```

仍註冊：

* `計算科目成績`
* `計算領域成績`
* `計算學習領域成績`

不再由本模組註冊：

* `加總學習領域文字描述`

本次未啟動主程式做實際畫面點選。

## 8. 批次選單驗證

靜態檢查 `Program.cs`：

```text
教務作業 → 批次作業/檢視 → 成績作業
```

仍註冊：

* `批次計算科目成績`
* `批次計算領域成績`

不再由本模組註冊：

* `批次計算學習領域成績`
* `批次加總學習領域文字描述`

本次未啟動主程式做實際畫面點選。

## 9. 其他模組是否也註冊了停用項目

搜尋 `JHEvaluation` 全庫（排除 `bin` / `obj`）中的選單名稱與類別名稱：

* `加總學習領域文字描述`
* `批次計算學習領域成績`
* `批次加總學習領域文字描述`
* `DomainTextScoreSum`
* `LearningDomainScoreCalculateByGradeyear`
* `DomainTextScoreSumByGradeyear`

目前可執行的註冊只出現在本專案 `Program.cs`，且已註解。未發現其他模組註冊相同選單項目。類別本體只存在於本專案，未改動。

## 10. 測試中發現的問題

無。三筆註冊已停用，保留功能未被修改，建置成功。
