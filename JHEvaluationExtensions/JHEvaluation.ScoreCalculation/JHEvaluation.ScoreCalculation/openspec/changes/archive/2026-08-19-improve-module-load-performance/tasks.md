## 1. 載入盤點與量測基礎

- [x] 1.1 盤點 `Program.Main()` 的所有同步呼叫、框架初始化順序、UI 執行緒限制與宿主可用的 UI-ready／診斷機制，記錄各階段的必要性與可否延後
- [x] 1.2 建立既有功能驗證清單，涵蓋新竹／高雄模式、學生與教務 Ribbon、ACL、學生選取事件、報表、畢業功能、三個明細項目、批次學期歷程及資料合理性檢查
- [x] 1.3 實作低負擔的階段計時與彙整診斷元件，輸出固定階段識別、耗時、成功／失敗與例外資訊，並確保關鍵路徑不進行遠端記錄
- [x] 1.4 將計時包覆部署設定、學生功能、教務功能、畢業與報表、ACL／事件、明細項目、資料合理性檢查及整體載入階段
- [x] 1.5 驗證量測開啟與關閉時的功能結果一致，且量測元件本身不會吞掉初始化例外或把失敗記為成功

## 2. 建立最佳化前基準

- [ ] 2.1 固定測試版本、硬體、帳號、資料、部署模式與快取控制方式，為新竹及高雄模式各執行至少十次冷啟動與十次暖啟動
- [ ] 2.2 彙整總耗時與各階段的中位數、第 95 百分位及占比，排序主要瓶頸並標示重複物件存取、重複部署資訊取得或非必要工作
- [ ] 2.3 保存最佳化前基準報告，包含環境、樣本數、原始結果、統計方式、瓶頸證據與預定改善項目

## 3. 重構與低風險最佳化

- [x] 3.1 將 `Program.Main()` 拆成按責任命名且可獨立計時的初始化單元，維持原本執行順序與例外語意
- [x] 3.2 加入同一宿主生命週期只註冊一次的保護，驗證重複呼叫不會重複建立選單、掛載事件、加入明細項目或加入資料合理性檢查
- [x] 3.3 在各初始化單元快取重複使用的 Ribbon、選單、ACL 與部署參數結果，移除基準確認的重複查找與重複初始化
- [ ] 3.4 建置專案並逐項執行既有功能驗證清單，確認兩種部署模式的功能、權限與事件行為未改變

## 4. 實測瓶頸改善

- [ ] 4.1 重新量測低風險最佳化結果，僅針對仍顯著且符合安全條件的非必要階段選定延後初始化策略
- [ ] 4.2 為選定工作實作惰性首次使用或宿主 UI-ready 後初始化，保證必要 UI 與擴充點仍在 `Main()` 返回前完成註冊
- [ ] 4.3 為每項延後工作加入未開始、執行中、完成、失敗的單次狀態保護，將 UI 更新切回 UI 執行緒，並實作安全重試或明確停用結果
- [ ] 4.4 驗證同時觸發只執行一份初始化工作，延後工作失敗不會中止宿主或其他功能，且首次使用能取得一致結果與診斷資訊
- [ ] 4.5 量測首次使用延遲；若延後造成不可接受的互動延遲，改在 UI-ready 後預熱或恢復該階段的同步初始化

## 5. 效能與回歸驗收

- [ ] 5.1 以與基準相同的條件，為新竹及高雄模式各執行至少十次最佳化後冷啟動與十次暖啟動
- [ ] 5.2 產出前後比較報告；基準中位數高於 200 ms 時確認中位數至少降低 20% 且第 95 百分位不超過原值 110%，否則確認兩項統計皆不退化並列出已移除的重複工作
- [ ] 5.3 完整執行功能驗證清單與重複載入、延後初始化競態、失敗處理及 UI 執行緒測試，記錄結果
- [x] 5.4 執行 Release 建置與可用的自動化測試，處理警告或失敗，並確認未新增外部套件或公開 API／資料格式變更
- [ ] 5.5 將最終量測、已知限制、診斷使用方式與同步路徑回退步驟納入交付紀錄
